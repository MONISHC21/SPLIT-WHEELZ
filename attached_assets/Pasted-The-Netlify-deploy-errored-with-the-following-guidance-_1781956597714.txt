The Netlify deploy errored, with the following guidance provided:

Diagnosis
- Relevant log lines: installing npm packages failed at [line 20](#L20), Netlify aborted with "dependency_installation script returned non-zero exit code" at [line 24](#L24), and npm reported an internal error "Exit handler never called!" at [line 25](#L25). The npm debug log location is shown at [line 28](#L28). The build then fails to install dependencies ([line 29](#L29), [line 30](#L30)).
- Error type & likely cause: npm itself failed during install ("Exit handler never called!"). This is typically caused by an incompatible Node / npm combination (Netlify installed Node v22.23.0 with npm v10.9.8 per the logs) or by an install script/lockfile interaction that hangs/crashes the npm CLI. Most reliable fix is to run the build with a known LTS Node/npm combo (for example Node 18 or 20) or to inspect the npm debug log for the specific failing package/script.

Before changing Netlify build settings, verify your repo structure
- Make sure the frontend package.json is present and committed at splitwheelz/frontend/package.json:
  - https://github.com/MONISHC21/SPLIT-WHEELZ/blob/main/splitwheelz/frontend/package.json
- If package.json is missing or in a different folder, Netlify will try to run npm where there’s nothing to install—fix the path or add package.json.

Recommended solution (step‑by‑step)
1. Try pinning Node to an LTS version that is known stable with current npm (Node 18 or 20)
   - Option A — add a .nvmrc at your repo root (or in splitwheelz/frontend) containing the Node major version you want, e.g.:
```text
18
```
   - Option B — add "engines" to splitwheelz/frontend/package.json and commit:
```json
"engines": {
  "node": ">=18 <21"
}
```
   - Option C — set the Node version in Netlify UI: build & deploy > Environment > edit NODE_VERSION (for example set to 18.18.0).
   - Netlify docs for changing Node versions: https://docs.netlify.com/configure-builds/manage-dependencies/#node-js-and-javascript

2. Clear the Netlify build cache and re-trigger the deploy
   - In Netlify UI choose "Clear cache and deploy site" so the new Node version and a fresh install run.

3. If the error persists, inspect the npm debug log referenced in the build (the path in the logs). It contains the stack trace / package script that caused npm to fail:
   - See the log path printed in the build output (for example at [line 28](#L28)).
   - If a postinstall or prepare script from a dependency is failing, either fix/remove that script or update the dependency.

4. Optional workarounds if you cannot change Node immediately
   - Use a different install command in Netlify Build settings (Build > Edit settings > Build command) such as:
     - npm ci --prefer-offline --no-audit
     - Or pre-upgrade/downgrade npm in a build command: npm install -g npm@9 && npm ci
   - But prefer changing Node to an LTS version instead of patching npm globally.

5. If you still see "Exit handler never called!" after trying an LTS Node:
   - Share the npm debug log (the full contents of the file referenced at [line 28](#L28)). That log will show the actual failing package or script and I can give a precise fix.

Summary
- Root cause: npm CLI crashed during install (likely Node 22 + npm 10 incompatibility or a failing install script).
- Primary fix: pin to Node LTS (18 or 20) via .nvmrc, package.json engines, or Netlify NODE_VERSION, clear cache, and re-deploy. Docs: https://docs.netlify.com/configure-builds/manage-dependencies/#node-js-and-javascript

The relevant error logs are:

Line 0: build-image version: cbbb2660d860fe70694043ec05851a3910a2bf39 (noble-new-builds)
Line 1: buildbot version: 12b3c6f417c1b3440df5ed3246d8285499c647e3
Line 2: Fetching cached dependencies
Line 3: Failed to fetch cache, continuing with build
Line 4: Starting to prepare the repo for build
Line 5: No cached dependencies found. Cloning fresh repo
Line 6: git clone --filter=blob:none https://github.com/MONISHC21/SPLIT-WHEELZ
Line 7: Preparing Git Reference refs/heads/main
Line 8: Custom publish path detected. Proceeding with the specified path: 'splitwheelz/frontend'
Line 9: Installing dependencies
Line 10: mise ~/.config/mise/config.toml tools: python@3.14.3
Line 11: mise ~/.config/mise/config.toml tools: ruby@3.4.8
Line 12: mise ~/.config/mise/config.toml tools: go@1.26.2
Line 13: Downloading and installing node v22.23.0...
Line 14: Downloading https://nodejs.org/dist/v22.23.0/node-v22.23.0-linux-x64.tar.xz...
Line 15: Computing checksum with sha256sum
Line 16: Checksums matched!
Line 17: Now using node v22.23.0 (npm v10.9.8)
Line 18: Enabling Node.js Corepack
Line 19: No npm workspaces detected
Line 20: Installing npm packages using npm version 10.9.8
Line 21: npm warn deprecated inflight@1.0.6: This module is not supported, and leaks memory. Do not use it. Check out lru-cache if you wa
Line 22: npm warn deprecated rimraf@3.0.2: Rimraf versions prior to v4 are no longer supported
Line 23: npm warn deprecated glob@7.2.3: Old versions of glob are not supported, and contain widely publicized security vulnerabilities, 
Line 24: Failed during stage 'Install dependencies': dependency_installation script returned non-zero exit code: 1
Line 25: npm error Exit handler never called!
Line 26: npm error This is an error with npm itself. Please report this error at:
Line 27: npm error   <https://github.com/npm/cli/issues>
Line 28: npm error A complete log of this run can be found in: /opt/buildhome/.npm/_logs/2026-06-20T11_54_30_353Z-debug-0.log
Line 29: Error during npm install
Line 30: Failing build: Failed to install dependencies
Line 31: Finished processing build request in 1m16.634s